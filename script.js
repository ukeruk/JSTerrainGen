const size = 500;
var ns;
noise.seed(Math.random());

height_map = [];

for (let x = 0; x < size; x++) {
    height_map[x] = [];
    for (let y = 0; y < size; y++) {
        height_map[x][y] = 
        
    }
    
}